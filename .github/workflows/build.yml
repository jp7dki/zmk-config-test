jobs:
  build:
    runs-on: ubuntu-latest
    name: Build Test
    strategy:
      matrix:
        include:
          # もともと書いてある設定（例：nice_nano_v2など）の下に追加します
          - board: seeeduino_xiao_ble
            shield: onekey